{
  "name": "claude-agent-server-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "npm run start --workspace=@claude-agent/server",
    "start:sandbox": "npm run start --workspace=@claude-agent/server",
    "build:e2b": "npm run build --workspace=@claude-agent/e2b-build",
    "test:client": "npm run test --workspace=@dzhng/claude-agent"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.4.1",
    "@types/node": "^22",
    "prettier": "^3.6.2",
    "tsx": "^4",
    "typescript": "^5"
  }
}
